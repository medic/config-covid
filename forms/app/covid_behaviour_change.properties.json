{
    "icon": "icon-person",
    "title": "Covid Education: Behaviour change",
    "context": {
        "person": false,
        "place": false,
        "expression": "!contact || (contact.type === 'person' && user.parent.type === 'health_center' && user.role === 'chw' && contact.role === 'chw')"
    }
}