{
    "icon": "icon-person",
    "title": "EBS Training",
    "context": {
        "person": true,
        "place": false,
        "expression": "!contact || (contact.type === 'person' && user.parent.type === 'health_center' && user.role === 'chw' && contact.role === 'chw' && summary.hasCompletedEBSTraining)"
    }
}